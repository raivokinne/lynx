let todos = [];

let addTask = fn(name) {
    todos.push({ "name": name, "done": false });
}

let completeTask = fn(i) {
    todos[i].done = true;
}

addTask("Write code");
addTask("Test program");
completeTask(0);

print(todos);

