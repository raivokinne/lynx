let get = fn(url, headers) {
    if http_get == null { error "net.get not available" }
    return http_get(url, headers)
}

let post = fn(url, body, headers) {
    if http_post == null { error "net.post not available" }
    return http_post(url, body, headers)
}
